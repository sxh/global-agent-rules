// Always-injected PCP behavioral rule — see the PCP_CACHE_FIX note in pcp.ts.
//
// Kept in its own module so it is unit-testable: the rule is configuration with
// an observable outcome (appended to every system prompt), and pcp-rule.test.mjs
// guards the batch-loading and post-pivot queue-advance guidance against silent
// removal.

export const PCP_RULE = `[PCP RULE] Task language: follow the user's communication language (Chinese for Chinese, English for English). Task granularity: each Task = one concrete deliverable (≤2h, with a completion criterion); never create project-goal / sprint-container mega-tasks. Use pcp_sub only for a temporary detour (return immediately when done); never use pcp_sub to execute a queued Task. [Completion review] When a task finishes with output files → list them and ask "needs review?" → if yes, present by type (.md → convert to PDF with pandoc and give the path; .json → format key fields; .txt → paste short files or summarize long ones; code → git diff key changes) → call pcp_done after confirmation; if no review is needed, call pcp_done directly. "later / by the way / note X" → pcp_capture. On receiving a todolist/plan → first scan the project's existing code and outputs, create no tasks for work already done → load with pcp_plan(tasks), show the list, and wait for user confirmation before executing. "originally / was going to / changed to / found something better" → confirm whether to pcp_pivot. No active task → guide the user to make a plan. Close a task by calling pcp_done before its closing commit, then stage .opencode/pcp in that same commit (one commit per task); a PCP-Task trailer auto-closes only as a fallback if pcp_done was not called. Batch-load tasks with pcp_plan in order; pcp_promote appends backlog items to the sprint queue and they run in FIFO order after the active task. After a pivot with no active task, use pcp_start to advance the queue head; never mint a duplicate id.`;
